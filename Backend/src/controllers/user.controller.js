// Login and Signup , Logout
// Update SocketId after every login
// Forget Password
// Delete Account

const signUp = asyncHandler((req, resp, next)=>{});
const logIn = asyncHandler((req, resp, next)=>{});
const logOut = asyncHandler((req, resp, next)=>{});
const deleteAccount = asyncHandler((req, resp, next)=>{});
const forgetPassword = asyncHandler((req, resp, next)=>{});
const updateSocketId = asyncHandler((req, resp, next)=>{});